      <header className="center" style={{ display: "none" }}>
        <Image
          src="https://docrdsfx76ssb.cloudfront.net/static/1642175923/pages/wp-content/uploads/2021/08/bitly_logo.svg"
          alt="logo"
          width={100}
          height={50}
          className="logo"
        />
        <div className="flex">
          <nav>
            <ul className="flex">
              {navLiksLeft.map((link) => (
                <li className="mr-6" key={link}>
                  {link}
                </li>
              ))}
            </ul>
          </nav>
          <nav className="ml-auto">
            <ul className="flex">
              {navLiksRight.map((link) => (
                <li className="mr-6" key={link}>
                  {link}
                </li>
              ))}
              <button>Get a Quote</button>
            </ul>
          </nav>
        </div>
      </header>