import styles from "../styles/Home.module.css";

const Contact = ({ story, preview }) => {
  return (
    <div className={styles.container}>
      <header>
        <h1>Contact Page</h1>
      </header>

      <p>This is the contact page</p>
    </div>
  );
};

export default Contact;
